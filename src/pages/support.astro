---
import Layout from '../layouts/Layout.astro';
import apps from '../data/apps.json';

// Get app from URL query parameter
const appParam = Astro.url.searchParams.get('app');
const selectedApp = apps.find(a => a.name === appParam);
---

<Layout 
  title="Support | Hot Baked Apps"
  description="Get help with Hot Baked Apps products. Contact our support team for assistance."
  canonicalPath="/support"
>
  <section class="support-page">
    <div class="support-container">
      <header class="support-header">
        <span class="support-icon">💬</span>
        <h1>How can we help?</h1>
        <p class="support-subtitle">
          We're here to assist you with any questions or issues you might have.
        </p>
      </header>

      <form 
        id="support-form" 
        class="support-form"
        action="https://api.web3forms.com/submit" 
        method="POST"
      >
        <input type="hidden" name="access_key" value="4ad3e884-5a01-45a8-9327-62e267ab9e5b" />
        <input type="hidden" name="subject" id="form-subject" value="Support Request" />
        <input type="hidden" name="from_name" value="Hot Baked Apps Support Form" />
        <input type="checkbox" name="botcheck" class="hidden" style="display: none;" />

        <div class="form-group">
          <label for="request-type">What can we help with?</label>
          <div class="select-wrapper">
            <select name="request_type" id="request-type" required>
              <option value="">Select a topic...</option>
              <option value="support">General Support</option>
              <option value="bug">Report a Bug 🐛</option>
              <option value="feature">Feature Request 💡</option>
              <option value="other">Other</option>
            </select>
          </div>
        </div>

        <div class="form-group">
          <label for="app-select">Which app is this about?</label>
          <div class="select-wrapper">
            <select name="app" id="app-select">
              <option value="General">General / Not app-specific</option>
              {apps.map((app) => (
                <option value={app.name} selected={selectedApp?.slug === app.slug}>
                  {app.icon} {app.name}
                </option>
              ))}
            </select>
          </div>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="name">Your Name</label>
            <input type="text" name="name" id="name" placeholder="John Doe" required />
          </div>

          <div class="form-group">
            <label for="email">Your Email</label>
            <input type="email" name="email" id="email" placeholder="john@example.com" required />
          </div>
        </div>

        <div class="form-group">
          <label for="message">Message</label>
          <textarea 
            name="message" 
            id="message" 
            rows="5" 
            placeholder="Please describe your issue or request in detail..."
            required
          ></textarea>
        </div>

        <button type="submit" class="submit-btn">
          <span class="btn-text">Send Message</span>
          <span class="btn-loading">Sending...</span>
          <svg class="btn-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 2L11 13M22 2l-7 20-4-9-9-4 20-7z"/>
          </svg>
        </button>
      </form>

      <div id="success-message" class="success-message hidden">
        <div class="success-icon">✅</div>
        <h2>Message Sent!</h2>
        <p>Thanks for reaching out. We'll get back to you as soon as possible.</p>
        <button onclick="resetForm()" class="reset-btn">Send Another Message</button>
      </div>

      {selectedApp && (
        <div class="app-context">
          <p>
            <span class="app-context-icon">{selectedApp.icon}</span>
            Contacting support about <strong>{selectedApp.name}</strong>
          </p>
        </div>
      )}

      <div class="faq-section">
        <h2>Frequently Asked Questions</h2>
        
        <div class="faq-list">
          <details class="faq-item">
            <summary>How do I reset my app data?</summary>
            <p>
              All our apps store data locally on your device. To reset, you can delete 
              and reinstall the app, or check the app's settings for a "Reset Data" option.
            </p>
          </details>

          <details class="faq-item">
            <summary>Is my data backed up?</summary>
            <p>
              Our apps don't collect or store your data on our servers. Any data created 
              in the app stays on your device. We recommend using your device's built-in 
              backup features to protect your data.
            </p>
          </details>

          <details class="faq-item">
            <summary>How do I request a refund?</summary>
            <p>
              App purchases are handled through the App Store or Google Play. Please contact 
              Apple or Google directly for refund requests as we don't process payments directly.
            </p>
          </details>

          <details class="faq-item">
            <summary>When will new features be added?</summary>
            <p>
              We're constantly working to improve our apps! Follow us for updates, or sign up 
              for our newsletter to be the first to know about new features and releases.
            </p>
          </details>
        </div>
      </div>

      <footer class="support-footer">
        <a href="/" class="back-link">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M19 12H5M12 19l-7-7 7-7"/>
          </svg>
          <span>Back to Home</span>
        </a>
      </footer>
    </div>
  </section>
</Layout>

<script>
  const form = document.getElementById('support-form') as HTMLFormElement;
  const successMessage = document.getElementById('success-message');
  const requestType = document.getElementById('request-type') as HTMLSelectElement;
  const appSelect = document.getElementById('app-select') as HTMLSelectElement;
  const formSubject = document.getElementById('form-subject') as HTMLInputElement;
  const submitBtn = form?.querySelector('.submit-btn');

  // Update subject based on selections
  function updateSubject() {
    const typeLabels: Record<string, string> = {
      'support': 'Support Request',
      'bug': 'Bug Report',
      'feature': 'Feature Request',
      'other': 'Inquiry'
    };
    const type = typeLabels[requestType.value] || 'Support Request';
    const app = appSelect.value !== 'General' ? ` - ${appSelect.value}` : '';
    formSubject.value = `${type}${app}`;
  }

  requestType?.addEventListener('change', updateSubject);
  appSelect?.addEventListener('change', updateSubject);

  // Handle form submission
  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    submitBtn?.classList.add('loading');
    
    try {
      const response = await fetch(form.action, {
        method: 'POST',
        body: new FormData(form)
      });
      
      if (response.ok) {
        form.classList.add('hidden');
        successMessage?.classList.remove('hidden');
      } else {
        throw new Error('Form submission failed');
      }
    } catch (error) {
      alert('Something went wrong. Please try again.');
    } finally {
      submitBtn?.classList.remove('loading');
    }
  });

  // Reset form function
  (window as any).resetForm = () => {
    form.reset();
    form.classList.remove('hidden');
    successMessage?.classList.add('hidden');
  };
</script>

<style>
  .support-page {
    padding: 4rem 2rem;
    min-height: calc(100vh - 80px);
  }

  .support-container {
    max-width: 700px;
    margin: 0 auto;
  }

  .support-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .support-icon {
    font-size: 4rem;
    display: block;
    margin-bottom: 1.5rem;
  }

  .support-header h1 {
    font-size: 2.5rem;
    margin: 0 0 1rem;
  }

  .support-subtitle {
    color: var(--color-text-muted);
    font-size: 1.1rem;
    max-width: 500px;
    margin: 0 auto;
  }

  /* Form Styles */
  .support-form {
    background: linear-gradient(145deg, rgba(26, 26, 26, 0.8), rgba(13, 13, 13, 0.9));
    border: 1px solid rgba(245, 158, 11, 0.15);
    border-radius: 20px;
    padding: 2.5rem;
    margin-bottom: 2rem;
  }

  .support-form.hidden {
    display: none;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  label {
    display: block;
    color: var(--color-text);
    font-weight: 500;
    margin-bottom: 0.5rem;
    font-size: 0.95rem;
  }

  input[type="text"],
  input[type="email"],
  textarea,
  select {
    width: 100%;
    padding: 0.9rem 1rem;
    background: rgba(13, 13, 13, 0.8);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 10px;
    color: var(--color-text);
    font-size: 1rem;
    font-family: inherit;
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  input[type="text"]:focus,
  input[type="email"]:focus,
  textarea:focus,
  select:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(245, 158, 11, 0.1);
  }

  input::placeholder,
  textarea::placeholder {
    color: var(--color-text-muted);
    opacity: 0.6;
  }

  .select-wrapper {
    position: relative;
  }

  .select-wrapper::after {
    content: '▼';
    position: absolute;
    right: 1rem;
    top: 50%;
    transform: translateY(-50%);
    color: var(--color-text-muted);
    font-size: 0.7rem;
    pointer-events: none;
  }

  select {
    appearance: none;
    cursor: pointer;
    padding-right: 2.5rem;
  }

  select option {
    background: var(--color-bg);
    color: var(--color-text);
  }

  textarea {
    resize: vertical;
    min-height: 120px;
  }

  .submit-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    width: 100%;
    padding: 1rem 2rem;
    background: linear-gradient(135deg, var(--color-accent), #FBBF24);
    color: #0D0D0D;
    border: none;
    border-radius: 12px;
    font-weight: 600;
    font-size: 1.1rem;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 20px rgba(245, 158, 11, 0.3);
  }

  .submit-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(245, 158, 11, 0.4);
  }

  .submit-btn .btn-loading {
    display: none;
  }

  .submit-btn.loading .btn-text,
  .submit-btn.loading .btn-icon {
    display: none;
  }

  .submit-btn.loading .btn-loading {
    display: inline;
  }

  /* Success Message */
  .success-message {
    text-align: center;
    padding: 3rem;
    background: linear-gradient(145deg, rgba(26, 26, 26, 0.8), rgba(13, 13, 13, 0.9));
    border: 1px solid rgba(34, 197, 94, 0.3);
    border-radius: 20px;
    margin-bottom: 2rem;
  }

  .success-message.hidden {
    display: none;
  }

  .success-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
  }

  .success-message h2 {
    font-size: 1.75rem;
    margin: 0 0 0.75rem;
    color: #22C55E;
  }

  .success-message p {
    color: var(--color-text-muted);
    margin: 0 0 1.5rem;
  }

  .reset-btn {
    padding: 0.75rem 1.5rem;
    background: transparent;
    border: 1px solid var(--color-accent);
    color: var(--color-accent);
    border-radius: 10px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s;
  }

  .reset-btn:hover {
    background: var(--color-accent);
    color: #0D0D0D;
  }

  /* App Context */
  .app-context {
    background: rgba(245, 158, 11, 0.08);
    border: 1px solid rgba(245, 158, 11, 0.2);
    border-radius: 12px;
    padding: 1rem 1.5rem;
    margin-bottom: 3rem;
    text-align: center;
  }

  .app-context p {
    margin: 0;
    color: var(--color-text-muted);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
  }

  .app-context-icon {
    font-size: 1.5rem;
  }

  .app-context strong {
    color: var(--color-accent);
  }

  /* FAQ Section */
  .faq-section {
    margin-top: 4rem;
    padding-top: 3rem;
    border-top: 1px solid rgba(245, 158, 11, 0.15);
  }

  .faq-section h2 {
    font-size: 1.75rem;
    margin: 0 0 2rem;
    text-align: center;
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .faq-item {
    background: rgba(26, 26, 26, 0.5);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 12px;
    overflow: hidden;
  }

  .faq-item summary {
    padding: 1.25rem 1.5rem;
    cursor: pointer;
    font-weight: 600;
    color: var(--color-text);
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: color 0.2s;
  }

  .faq-item summary::-webkit-details-marker {
    display: none;
  }

  .faq-item summary::after {
    content: '+';
    font-size: 1.5rem;
    font-weight: 400;
    color: var(--color-accent);
    transition: transform 0.3s;
  }

  .faq-item[open] summary::after {
    transform: rotate(45deg);
  }

  .faq-item summary:hover {
    color: var(--color-accent);
  }

  .faq-item p {
    padding: 0 1.5rem 1.25rem;
    margin: 0;
    color: var(--color-text-muted);
    line-height: 1.7;
  }

  .support-footer {
    margin-top: 4rem;
    padding-top: 2rem;
    border-top: 1px solid rgba(245, 158, 11, 0.15);
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-text-muted);
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s;
  }

  .back-link:hover {
    color: var(--color-accent);
  }

  @media (max-width: 768px) {
    .support-page {
      padding: 2rem 1.5rem;
    }

    .support-header h1 {
      font-size: 2rem;
    }

    .support-form {
      padding: 1.5rem;
    }

    .form-row {
      grid-template-columns: 1fr;
    }
  }
</style>
